server:
  port: 9601
spring:
  profiles:
    active: dev
  application:
    name: smile-auth-resource
  cloud:
    nacos:
      discovery:
        server-addr: nacos1.4.2:8848
management:
  endpoints:
    web:
      exposure:
        include: "*"
