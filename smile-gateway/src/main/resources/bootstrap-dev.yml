

smile:
  gateway:
    ignore:
      urls: #配置白名单路径：可以从网关访问但是这几个匹配的URL可以不经过token认证
        - "/**/actuator/**"
        - "/auth/oauth/token"
        - "/smile-sys/user/**"
        - "/doc.html"
        - "/webjars/**"
        - "/swagger-resources/**"